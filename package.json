{
  "name": "terraform-github-aws-oidc",
  "version": "0.0.0-development",
  "description": "Terraform module for configuring authentication between GitHub Actions and AWS, using OpenID Connect.",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/capralifecycle/terraform-github-aws-oidc.git"
  },
  "scripts": {
    "semantic-release": "semantic-release",
    "upgrade-dependencies": "ncu --upgrade --install always --format group"
  },
  "devDependencies": {
    "npm-check-updates": "19.2.0",
    "semantic-release": "25.0.2"
  },
  "release": {
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/github"
    ]
  }
}
